# Frontend

React + TypeScript application using Tailwind CSS, Redux Toolkit, React Router v6, and Framer Motion. Pages include homepage, services, booking, team, patient portal, and shared layout components with multilingual support and real-time pricing updates via WebSockets. A theme provider manages dark/light mode, persisting the user's choice to local storage and rehydrating it on load while honoring OS-level preferences.
